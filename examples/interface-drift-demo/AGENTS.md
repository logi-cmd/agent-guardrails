# Demo Instructions

- Read `README.md` before changing the demo.
- Keep the example centered on one file under `src/orders/`.
- Treat this demo as a proof of interface drift, not a general API design example.
- Do not widen the task beyond `src/`, `tests/`, and `.agent-guardrails/`.
