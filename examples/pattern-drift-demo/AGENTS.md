# Demo Instructions

- Read `README.md` before changing the demo.
- Keep the example focused on one feature area under `src/orders/`.
- Treat `refund-service.ts` as the existing pattern to reuse.
- Do not widen the task beyond `src/`, `tests/`, and `.agent-guardrails/`.
