{
  "name": "pass",
  "description": "The corrected change set keeps the work inside src/ and tests/.",
  "task": "Add audit summary coverage to the app without changing deployment notes.",
  "allowedPaths": [
    "src/",
    "tests/"
  ],
  "requiredCommands": [
    "npm test"
  ],
  "evidencePaths": [
    ".agent-guardrails/evidence/current-task.md"
  ],
  "evidenceContent": "# Task Evidence\n\n- Task: Add audit summary coverage to the app without changing deployment notes.\n- Commands run: npm test\n- Notable results: Scope stayed inside src/ and tests/ and the required command was reported to check.\n- Residual risk: none\n",
  "commandsRun": [
    "npm test"
  ],
  "changedFiles": [
    "src/app.js",
    "tests/app.test.js"
  ]
}
