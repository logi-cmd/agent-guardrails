# v0.9.1

**Fix release — tests, dependencies, and repo cleanup.**

## What Changed

- Fixed `release.test.js` to match the rewritten enforce-first README
- Installed missing `@typescript-eslint/parser` dependency for plugin-ts
- Removed 6 commercial/strategy documents from public repo tracking
- Updated `zh-CN/README.md` to remove broken link

## Why

After the v0.9.0 README rewrite and enforce English fix, the release test suite was out of sync. Additionally, commercial strategy documents were accidentally committed to the public repo and needed to be gitignored.

## Upgrade

```bash
npm update -g agent-guardrails
```

No breaking changes. All existing enforce/unenforce behavior is identical to v0.9.0.
